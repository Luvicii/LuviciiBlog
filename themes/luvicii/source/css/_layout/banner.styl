.banners-title
  top: 2.9rem;
  left: 3rem;
  position: absolute;
  display: flex;
  flex-direction: column;

.banners-title-big 
  font-size: 36px;
  line-height: 1;
  font-weight: bold;
  margin-bottom: 8px;

.banners-title-small
  font-size: 12px;
  line-height: 1;
  color: var(--luvicii-secondtext);
  margin-top: 8px;
  margin-bottom: 0.5rem;

.banner-button 
  padding: 8px 12px;
  background: var(--luvicii-fontcolor);
  border-radius: 12px;
  color: var(--luvicii-card-bg);
  display: flex;
  align-items: center;
  z-index: 1;
  transition: 0.3s;
  cursor: pointer;
  box-shadow: var(--luvicii-shadow-black);
  &.secondary 
    background: var(--luvicii-secondbg);
    border: var(--style-border-always);
    color: var(--luvicii-lighttext);
    margin-right: 1rem;
    box-shadow: var(--luvicii-shadow-border);